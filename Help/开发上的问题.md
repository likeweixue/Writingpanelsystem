如果是新加功能，比如字典之类的，打算放在右边栏，如果边栏面板里面不显示内容，点击不了（没反应），是因为
compactDictionaryTree 元素找不到，因为面板还没有被添加到 DOM 中。
问题出在 openDictionarySidebar 函数中：它调用了 openToolSidebar('dictionary')，但 openToolSidebar 中还没有 dictionary 的 case 分支。
修复方案
在 outline.js 的 openToolSidebar 函数中添加 dictionary 的 case 分支：
找到 outline.js 中的 openToolSidebar 函数，在 switch(tool) 中添加 dictionary 分支：

因为
openToolSidebar 是"总开关"openToolSidebar 函数是所有工具的统一入口。它负责：
1，创建浮动面板容器 (div#floatingToolPanel)
2，根据 tool 参数决定渲染哪个工具的内容
3，管理面板的打开/关闭/切换

所有outline.js（大纲很重要），所有的边栏的布局都是基于大纲面板的
—————————————————————————————————————————完